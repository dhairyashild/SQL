################  remember 1st 2 line only

*DDL (Data Definition Language) :         CARD T  TABLE              - every ddl has TABLE after card t
**DML (Data Manipulation Language):       SIDU                       - TABLENAME DIRECT  - NO TABLE
                                     S-from-where      D-from-where    U-s-where    I-into-valueS 


-- DATABASE CREATION AND NAVIGATION
CREATE DATABASE my_db; -- Creates a new database named 'my_db'
SHOW DATABASES;        -- Lists all available databases
USE my_db;             -- Selects 'my_db' for active use   - use tablename not exist =my error
SHOW TABLES;           -- Lists all tables within the selected database
-------------------------------------------------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------------------------------------------------
*DDL (Data Definition Language) =         CARD T

* `CREATE`:              
CREATE TABLE Emp (                                         # AFTER TABLENAME SPACE GIVEN/NOT GIVEN ITS OK
    Emp_ID int NOT NULL,               -- IMP: Cannot be empty
    Name varchar(20) NOT NULL,         -- IMP: Name required
    Age int NOT NULL,                  -- IMP: Age required
    Address varchar(100) DEFAULT 'India', -- IMP: Auto-fills 'India' if blank
    Salary numeric(10,2) NOT NULL,     -- IMP: Decimal salary required
    DeptID int,                        -- IMP: Reference column
    PRIMARY KEY(Emp_ID),               -- IMP: Unique ID for the table
    FOREIGN KEY(DeptID) REFERENCES Dept(DeptID), -- IMP: Links to Dept table
    CHECK(Age >= 18)                   -- IMP: Rule: Age must be 18+
);
-------- INSPECTING TABLE STRUCTURE
DESC branch; -- Displays column names, data types, and constraints


* ALTER:ADMR  ALTER TABLE Emp ADD Email varchar(50);ALTER TABLE Emp RENAME COLUMN Address TO Home_Address;
              ALTER TABLE Emp DROP COLUMN Email;  ALTER TABLE Emp MODIFY COLUMN Name varchar(50) NOT NULL;

* `DROP`:           DROP TABLE Emp;              DROP DATABASE my_db;

* `TRUNCATE`:       TRUNCATE TABLE Emp;

* `RENAME`:         RENAME TABLE Emp TO Employee;

-------------------------------------------------------------------------------------------------------------------------------------------
**DML (Data Manipulation Language):                 SIDU           S-from-where      D-from-where    U-s-where    I-into-valueS 


-- 2. SELECT: Retrieves data from a database
-- TABLE: naukar (Sample Data)
/*
| name    | salary | department  | job_role  | location |
|---------|--------|-------------|-----------|----------|
| Dheeraj | 80000  | Engineering | Developer | Mumbai   |
| Amit    | 75000  | Engineering | Developer | Pune     |
| Priya   | 90000  | Engineering | Manager   | Mumbai   |
| Rahul   | 60000  | HR          | HR        | Pune     |
| Sneha   | 80000  | Engineering | Developer | Mumbai   |
*/

-- 1. View entire table
SELECT * FROM naukar; 
------> Returns all 5 rows and 5 columns shown above.

-- 2. Filter by Engineering department
SELECT name, salary FROM naukar WHERE department = 'Engineering';
------> Dheeraj | 80000
------> Amit    | 75000
------> Priya   | 90000
------> Sneha   | 80000

-- 3. Unique job roles
SELECT DISTINCT job_role FROM naukar;
------> Developer
------> Manager
------> HR

-- 4. Count of unique job roles - below both commands same
SELECT COUNT(DISTINCT job_role) AS total_unique_roles FROM naukar;  
SELECT COUNT(DISTINCT job_role) FROM naukar;       # work without-->   AS total_unique_roles

------> 3                                 

-- 5. Unique combinations of Role and Location
SELECT DISTINCT job_role, location FROM naukar;
------> Developer | Mumbai
------> Developer | Pune
------> Manager   | Mumbai
------> HR        | Pune
-- (Note: Sneha's 'Developer | Mumbai' is excluded because it's a duplicate of Dheeraj's combination)

-------------------------------

INSERT INTO employees (id, name, department, salary)
VALUES (101, 'Alice Smith', 'Engineering', 85000);

-------------------------------
-- 4. DELETE: Removes rows from a table
DELETE FROM employees 
WHERE id = 101;                                         # ROW NUM TO DELETE

-------------------------------

-- 3. UPDATE: Modifies existing data within a table
UPDATE employees 
SET salary = 90000                                      # SET COLOMN = NEW VALUE
WHERE id = 101;                                         # ROW  NUMBER

-------------------------------------------------------------------------------------------------------------------------------------------












